using UnityEngine;
using System.Collections;

public class OutOfBound : MonoBehaviour {

    private readonly Vector2 gameSize = new Vector2(90, 74);

	// Update is called once per frame
	void Update () {
      //  if(trans)	
	}
}
